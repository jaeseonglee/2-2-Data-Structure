import java.util.Arrays;

public class Q08 {

	public static void main(String[] args) {
		int[] arr1 = {1,1,2}; // 정렬된 배열. 중복된 값이 있음
		int[] arr2 = {0,0,1,1,1,2,2,3,3,4};  // 정렬된 배열. 중복된 값이 있음

		System.out.println("arr1 : ");
		System.out.println("- before : " + Arrays.toString(arr1));
		System.out.println("- after : " + Arrays.toString(cleanup(arr1)));

		System.out.println("arr2 : ");
		System.out.println("- before : " + Arrays.toString(arr2));
		System.out.println("- after : " + Arrays.toString(cleanup(arr2)));
	}
	public static int[] cleanup(int[] arr) {
		/*
		 * 정렬된 int 배열이 입력으로 주어집니다.
		 * cleanup 함수는 정렬된 배열에서 중복되는 값을 제거한 새로운 배열을 리턴합니다.
		 * 즉, 중복된 값이 있으면 그 중에서 한개만 남기고 나머지는 제거합니다.
		 * 예) 입력 : {1,1,2} => 출력 {1,2}
		 * 예) 입력 : {1,1,2,2} => 출력 {1,2}
		 * 예) 입력 : {1,1} => 출력 {1}
		 */
		
		
		/* 여기에 들어갈 코드를 작성하세요 */
	
		
		
	}
}
